<%= content_tag :h1, "View Row##{@row.id}" %>
<p>Title: <%= @row.title %></p>
<p>Description:<br><%= @row.description %></p>
<p>Link url: <%= @row.link_url %></p>

<div class="thumbs">
  <%= render @row.thumbs %>
</div>

<%= link_to 'Create new thumb', new_row_thumb_path(@row) %>